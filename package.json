{
  "name": "peeji",
  "version": "0.0.0",
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "turbo": "^1.6.3"
  },
  "scripts": {
    "build": "turbo run build",
    "test": "turbo run test",
    "lint": "turbo run lint",
    "dev": "turbo run dev"
  },
  "dependencies": {
    "@docusaurus/theme-live-codeblock": "^2.2.0"
  }
}
